# よく使うコマンド

## 🚀 開発開始
```bash
cd /Users/valan/Desktop/rag-knowledge-api
docker compose up
# → http://localhost:8000/docs
```

## ⏹️ 開発終了
```bash
# Ctrl+C でサーバー停止
docker compose down
```

## 🧪 テスト実行
```bash
poetry run pytest -v
```

## 🔄 コード変更後

Docker起動中なら自動反映 → ブラウザをリロード

## 📦 パッケージ追加
```bash
poetry add パッケージ名
docker compose up --build
```

## 🗑️ DB リセット
```bash
docker compose down -v
docker compose up
```

## 📝 Git
```bash
git add .
git commit -m "メッセージ"
git push
```

## 🆘 トラブル
```bash
# Dockerが動かない
docker compose down
docker compose up --build

# ポートが使われてる
lsof -ti:8000 | xargs kill -9
```